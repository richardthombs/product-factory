# Generated from /product-events.
# Do not edit directly.
# To change this file, add or modify product events.
capabilities:
  - capability_id: CAP-001
    capability_name: Manage product model structure
    features:
      - feature_id: FEAT-001
        feature_name: Create initial product
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-001
                tests:
                  - TEST-001
                text: Given an empty event store, when the create-product command is run, then a ProductCreated event is written and the product model is rebuilt.
            description: The system shall create an initial product record when the event store is empty.
            requirement_id: REQ-001
        status: active
      - feature_id: FEAT-002
        feature_name: Create capability
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-002
                tests:
                  - TEST-002
                text: Given an existing product model, when the create-capability command is run, then a CapabilityAdded event is written and the product model is rebuilt.
            description: The system shall create a capability under an existing product model.
            requirement_id: REQ-002
        status: active
      - feature_id: FEAT-003
        feature_name: Create feature with requirements and acceptance criteria
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-003
                tests:
                  - TEST-003
                text: Given an existing capability, when the create-feature command is run with one acceptance criterion, then the system writes FeatureAdded, RequirementAdded, and AcceptanceCriterionAdded events and rebuilds the model.
              - acceptance_criterion_id: AC-004
                tests:
                  - TEST-004
                text: Given an existing capability, when the create-feature command is run with multiple acceptance criterion texts, then the system creates one acceptance criterion event for each text.
            description: The system shall create a feature, a linked requirement, and one or more linked acceptance criteria for an existing capability.
            requirement_id: REQ-003
        status: active
      - feature_id: FEAT-008
        feature_name: Create requirement
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-010
                tests:
                  - TEST-010
                text: Given an existing feature, when the create-requirement helper is run, then the system writes a RequirementAdded event and one AcceptanceCriterionAdded event per acceptance criterion and rebuilds the model.
              - acceptance_criterion_id: AC-011
                tests:
                  - TEST-011
                text: Given an existing feature, when the create-requirement helper is run with multiple acceptance criteria, then the new requirement is projected under the feature with all created acceptance criteria.
            description: The system shall create a requirement under an existing feature and attach one or more acceptance criteria to it.
            requirement_id: REQ-008
        status: active
      - feature_id: FEAT-009
        feature_name: Change existing requirements and acceptance criteria
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-012
                tests:
                  - TEST-013
                text: Given an existing requirement, when the change-requirement helper is run with a new description, then the system records a RequirementChanged event and projects the updated requirement description.
              - acceptance_criterion_id: AC-013
                tests:
                  - TEST-014
                text: Given an existing acceptance criterion, when the change-acceptance-criterion helper is run with new text, then the system records an AcceptanceCriterionChanged event and projects the updated acceptance criterion text.
            description: The system shall provide helpers to change existing requirements and acceptance criteria without rewriting accepted history.
            requirement_id: REQ-009
        status: active
      - feature_id: FEAT-010
        feature_name: Reshape feature structure
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-014
                tests:
                  - TEST-015
                text: Given an existing feature and a target capability, when the move-feature helper is run, then the system records a FeatureMovedToCapability event and projects the feature under the target capability.
              - acceptance_criterion_id: AC-015
                tests:
                  - TEST-016
                text: Given an existing feature, when the deprecate-feature helper is run with a reason, then the system records a FeatureDeprecated event and projects the feature as deprecated with its deprecation reason.
            description: The system shall support moving an existing feature to another capability and deprecating an existing feature without deleting its history.
            requirement_id: REQ-010
        status: active
      - feature_id: FEAT-011
        feature_name: Track capability and feature readiness
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-016
                tests:
                  - TEST-017
                text: Given an existing feature, when the set-feature-status helper is run with a new status, then the system records a FeatureStatusChanged event and projects the updated feature readiness state.
              - acceptance_criterion_id: AC-017
                tests:
                  - TEST-018
                text: Given an existing capability, when the set-capability-status helper is run with a new status, then the system records a CapabilityStatusChanged event and projects the updated capability readiness state.
            description: The system shall record capability and feature status changes and project the resulting readiness metadata.
            requirement_id: REQ-011
        status: active
      - feature_id: FEAT-014
        feature_name: Change existing feature descriptions
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-026
                tests:
                  - TEST-029
                text: Given an existing feature, when the change-feature helper is run with a new description, then the system records a FeatureChanged event and projects the updated feature description.
            description: The system shall provide a helper to change an existing feature description without rewriting accepted history.
            requirement_id: REQ-016
        status: active
  - capability_id: CAP-002
    capability_name: Validate and project product model
    features:
      - feature_id: FEAT-004
        feature_name: Validate product events
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-005
                tests:
                  - TEST-005
                text: Given a set of product event files, when validate-events is run, then the system reports schema or reference errors and succeeds only when the event stream is valid.
            description: The system shall validate product event files against schemas and repository-level consistency rules.
            requirement_id: REQ-004
          - acceptance_criteria:
              - acceptance_criterion_id: AC-032
                tests:
                  - TEST-036
                text: Given concurrency preconditions that reference existing entities and prior events affecting those same entities, when validate-events is run, then the event stream is accepted as valid structured concurrency metadata.
              - acceptance_criterion_id: AC-033
                tests:
                  - TEST-037
                text: Given concurrency preconditions that reference missing entities or last-event ids that did not affect the referenced entity, when validate-events is run, then it reports validation errors.
            description: The system shall validate optional concurrency metadata for product events, including entity-specific revision and last-entity-event preconditions.
            requirement_id: REQ-019
        status: active
      - feature_id: FEAT-005
        feature_name: Project current-state product model
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-006
                tests:
                  - TEST-006
                  - TEST-008
                text: Given a valid event stream, when project-model is run, then the system generates deterministic YAML files for the product, capabilities, features, requirements, acceptance criteria, and indexes.
            description: The system shall deterministically project accepted product events into generated YAML documentation of current state.
            requirement_id: REQ-005
          - acceptance_criteria:
              - acceptance_criterion_id: AC-034
                tests:
                  - TEST-030
                text: Given accepted events affecting capabilities, features, requirements, acceptance criteria, or tests, when project-model is run, then the projected model includes deterministic derived revision metadata for those entities.
            description: The system shall derive deterministic entity revisions and last affecting event ids during replay and projection for accepted entities.
            requirement_id: REQ-020
        status: active
      - feature_id: FEAT-006
        feature_name: Rebuild product model
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-007
                tests:
                  - TEST-012
                text: Given a valid event stream, when rebuild is run, then the system validates events and regenerates the product model successfully.
            description: The system shall provide a rebuild command that validates events and regenerates the product model.
            requirement_id: REQ-006
        status: active
      - feature_id: FEAT-007
        feature_name: Create tests for acceptance criteria
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-008
                tests:
                  - TEST-009
                text: Given one or more acceptance criterion ids, a test file, and a test name, when the create-test helper is run, then the test is annotated with the acceptance criterion ids and one TestCreated event is recorded for each id.
              - acceptance_criterion_id: AC-009
                tests:
                  - TEST-007
                text: Given created tests and acceptance criteria, when project-model is run, then the generated model includes test ids under the relevant acceptance criteria and projects each test artifact with file path and test name.
            description: The system shall provide a create-test helper that annotates tests with acceptance criterion ids and records test artifacts with test id, file path, and test name.
            requirement_id: REQ-007
        status: active
      - feature_id: FEAT-012
        feature_name: Report branch delta
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-018
                tests:
                  - TEST-019
                text: Given a working branch with product events not contained in the base branch, when the branch-delta helper is run, then it reports the branch-only product events relative to the base branch.
              - acceptance_criterion_id: AC-019
                tests:
                  - TEST-020
                text: Given a base branch that cannot be resolved, when the branch-delta helper is run, then it fails instead of emitting a trusted branch-delta report.
            description: The system shall compare the current branch against a base branch and report the product events unique to the current branch.
            requirement_id: REQ-012
          - acceptance_criteria:
              - acceptance_criterion_id: AC-020
                tests:
                  - TEST-021
                text: Given branch-only product events affecting capabilities, features, requirements, acceptance criteria, or tests, when the branch-delta helper is run, then it reports changed entities grouped by entity type and change kind based on their net difference from the base branch.
              - acceptance_criterion_id: AC-021
                tests:
                  - TEST-022
                text: Given entities that are added and then further refined only on the working branch, when the branch-delta helper is run, then it reports them as added and infers change categories from the net branch delta rather than intermediate branch-only churn.
            description: The system shall summarize net changed product entities and inferred change categories in the branch-delta report relative to the base branch.
            requirement_id: REQ-013
          - acceptance_criteria:
              - acceptance_criterion_id: AC-022
                tests:
                  - TEST-023
                  - TEST-024
                text: Given net changed entities in the branch delta, when the branch-delta helper is run, then it reports the impacted capabilities, features, requirements, acceptance criteria, and tests related to those changes.
              - acceptance_criterion_id: AC-023
                tests:
                  - TEST-025
                  - TEST-026
                text: Given a working branch and base branch, when the branch-delta helper is run without a custom output path, then it writes branch-delta/branch-delta.yaml and branch-delta/branch-delta.md as gitignored branch-local artifacts, and the markdown presents changed entities in parent context with explicit change labels.
            description: The system shall report impacted product entities and write standard branch-delta artifacts for the current branch based on the net branch delta.
            requirement_id: REQ-014
        status: active
      - feature_id: FEAT-013
        feature_name: Derive work packages from branch delta
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-024
                tests:
                  - TEST-027
                text: Given a net branch delta with changed entities under one feature, when the derive-work-packages helper is run, then it emits a work-package proposal that groups that net changed scope under that feature.
              - acceptance_criterion_id: AC-025
                tests:
                  - TEST-028
                text: Given a net branch delta with changed entities, when the derive-work-packages helper is run, then each derived work package includes its scoped capability, feature, requirement, acceptance-criterion, and test ids together with rationale and dependency information for that net changed scope.
            description: The system shall derive deterministic work-package proposals from the net branch delta by grouping net changed scope into coherent implementation slices.
            requirement_id: REQ-015
        status: active
      - feature_id: FEAT-015
        feature_name: Reconcile branch-only events
        requirements:
          - acceptance_criteria:
              - acceptance_criterion_id: AC-027
                tests:
                  - TEST-031
                  - TEST-038
                text: Given branch-only events whose concurrency preconditions match the latest accepted state, when the reconcile-events helper is run against a base branch, then it exits successfully without writing reconciliation artifacts and without producing stdout or stderr.
              - acceptance_criterion_id: AC-028
                tests:
                  - TEST-032
                text: Given a branch-only event whose expected entity revision or expected last entity event id no longer matches the latest accepted state for that same entity, when reconcile-events is run, then it reports a reconciliation conflict and fails.
              - acceptance_criterion_id: AC-029
                tests:
                  - TEST-033
                text: Given an unrelated accepted event for another entity, when reconcile-events compares expected_last_entity_event_id for a branch event, then it compares only against the most recent accepted event that affected the referenced entity.
            description: The system shall reconcile branch-only product events against the latest accepted entity revisions on a chosen base branch.
            requirement_id: REQ-017
          - acceptance_criteria:
              - acceptance_criterion_id: AC-030
                tests:
                  - TEST-034
                  - TEST-039
                text: Given reconcile-events finds reconciliation conflicts and is run without a custom output path, when it completes, then it writes branch-delta/reconciliation.yaml and branch-delta/reconciliation.md as gitignored branch-local artifacts.
              - acceptance_criterion_id: AC-031
                tests:
                  - TEST-035
                text: Given reconciliation against the latest target branch fails, when reconcile-events is run in local workflow or CI, then it exits non-zero so merge gating can block acceptance.
            description: The system shall write standard reconciliation artifacts and support merge-gate enforcement for stale branch proposals.
            requirement_id: REQ-018
        status: active
product_id: PROD-001
projected_from:
  event_count: 126
  last_event_id: EVT-20260601-0040
  last_occurred_at: 2026-06-01T09:26:30.000Z
  test_count: 39
